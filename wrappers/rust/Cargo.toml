[package]
name = "universal_timestamp"
version = "0.9.0"
edition = "2021"
description = "Safe Rust wrapper for the Universal Timestamp C library"
license = "MIT"
repository = "https://github.com/mozrin/universal_timestamp"

[dependencies]
# No external dependencies needed

[build-dependencies]

